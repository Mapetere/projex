{
  "name": "projex-legacy",
  "version": "0.2.0",
  "private": true,
  "description": "Event-driven portfolio inference system with UI",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @projex/app",
    "build": "npm run build -w @projex/core && npm run build -w @projex/app",
    "tauri": "npm run tauri -w @projex/app",
    "test": "npm run test -w @projex/core"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}